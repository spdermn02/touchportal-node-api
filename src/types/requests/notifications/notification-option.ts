/**
 * @property id - This is the id of the notification option. This id will be send back to the plug-in if the user selects the option.
 * @property title - This is the title of the notification option..
 */
export type NotificationOption = {
  id: string;
  title: string;
};
